{
  "name": "templ-demo",
  "version": "1.0.0",
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "@react-three/fiber": "^8.0.0",
    "@react-three/drei": "^9.0.0",
    "three": "^0.158.0",
    "react-loader-spinner": "^5.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "scripts": {
    "build": "esbuild --bundle index.js --outdir=dist --minify --global-name=bundle",
    "dev": "esbuild --bundle index.js --outdir=dist --watch --global-name=bundle"
  }
}
